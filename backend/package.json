{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon --env-file-if-exists=.env src/server.js",
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "migrate": "node --env-file-if-exists=.env scripts/migrate.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "//engines": "Sàn thật, không phải cho đẹp: `npm start` dùng cờ --env-file-if-exists, có từ Node 20.12. Không khai báo thì Render chạy Node mặc định của thời điểm tạo service, và nếu nó cũ hơn thì tiến trình chết ngay với 'bad option' — deploy fail mà app cũ vẫn chạy, nên nhìn bên ngoài không thấy gì khác. Trùng với Node mà CI dùng, để CI thực sự kiểm thử đúng phiên bản production chạy.",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cloudinary": "^2.8.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.2",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^9.9.1",
    "multer": "^2.0.2",
    "socket.io": "^4.8.1",
    "swagger-ui-express": "^5.0.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "mongodb-memory-server": "^10.1.4",
    "nodemon": "^3.1.14",
    "socket.io-client": "^4.8.3",
    "supertest": "^7.1.1",
    "vitest": "^3.2.4"
  }
}
